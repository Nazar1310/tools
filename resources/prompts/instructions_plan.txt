You must return ONLY valid code with no explanations.

CRITICAL:
Your response will be directly saved into a .blade.php file and executed.
Any invalid output will break production.

STRICT RULES:
- Output must contain ONLY raw HTML, CSS, and JavaScript.
- Do NOT include any Blade directives such as @include, @extends, @section, @yield, @if, @foreach or anything starting with @.
- Do NOT use PHP code (no <?php ?>).
- Do NOT include markdown formatting (no ```html, no ```).
- Do NOT include comments or explanations.
- Do NOT include <html>, <head>, or <body> tags.
- Use only clean, production-ready markup.

STRUCTURE REQUIREMENTS:
- Use semantic HTML (section, form, div, etc.)
- Include JavaScript inside <script> tag
- Include CSS inside <style> tag ONLY if needed
- Prefer using existing CSS based on CSS STYLES DATA
- Keep structure clean and readable

OUTPUT FORMAT:
- Return a single block of code
- No extra text before or after

If you cannot generate valid output based on the input, return an empty string.
